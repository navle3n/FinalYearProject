<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <Button
        android:id="@+id/select_image_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select Image"/>

    <ImageView
        android:id="@+id/preview_image_view"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_below="@id/select_image_btn"
        android:layout_marginTop="16dp"
        android:scaleType="centerCrop"/>

    <EditText
        android:id="@+id/user_email_edittext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/preview_image_view"
        android:layout_marginTop="16dp"
        android:hint="Enter user email"/>

    <Button
        android:id="@+id/send_image_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/user_email_edittext"
        android:layout_marginTop="16dp"
        android:text="Send Image"/>

    <Button
        android:id="@+id/view_shared_images_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/send_image_btn"
        android:layout_marginTop="16dp"
        android:text="View Images Shared With Me"/>

</RelativeLayout>

